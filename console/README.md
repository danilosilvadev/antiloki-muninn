# muninn console

Arrives in **slice 2** (D1 · console shell): Vite + React + TS, TanStack Router/Query,
antiloki design tokens, `strictPort 5177`, CORS allowlist exactly `localhost:5177`.

Empty by design until then — see the six-slice plan.
